# Exemple de configuration .env pour le projet
# Copiez ce fichier en ".env" à la racine du projet et renseignez les valeurs.

# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=true

# Twilio (désactivé par défaut en dev)
TWILIO_ENABLED=false
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# Expéditeur par défaut (un des trois suivants)
TWILIO_FROM=
TWILIO_FROM_SMS=
TWILIO_FROM_WHATSAPP=
# Optionnel: Messaging Service SID
TWILIO_MESSAGING_SERVICE_SID=
# Optionnel: callback de statut
TWILIO_STATUS_CALLBACK=

# Autres réglages utiles
PHONE_VERIFY_TTL_SECONDS=14400

# Synchronisation offline/online
# URL du service en ligne Render
MYSCHOOL_SYNC_SERVER_URL=https://gs-hadja-kanfing-dian.onrender.com
# Token secret cote serveur pour autoriser l'enregistrement des postes offline
MYSCHOOL_SYNC_ADMIN_TOKEN=
# Valeurs propres a chaque poste offline, generees par register_sync_device
MYSCHOOL_SYNC_DEVICE_ID=
MYSCHOOL_SYNC_TOKEN=
MYSCHOOL_SYNC_ECOLE_ID=
# Ecoute continue : duree pendant laquelle le serveur retient une requete pull.
# Doit rester sous le delai d'inactivite de l'hebergeur et du reverse proxy.
MYSCHOOL_SYNC_WAIT=25
# Plafond impose par le serveur, quelle que soit la demande du poste
MYSCHOOL_SYNC_LONGPOLL_MAX=30
# Frequence a laquelle le serveur regarde s'il a du neuf a livrer
MYSCHOOL_SYNC_LONGPOLL_INTERVAL=1.0
